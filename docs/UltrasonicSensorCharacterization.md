 / `creep drive begin` を記録する。前進可能距離の実測値が得られるまで、追加前進や斜め方向への探索移動は導入しない。

### 復路候補が元の接近方位へ戻るケース（14:50〜14:52）

`robot_log_20260912_145036.txt` は候補確認で325 mm・-68 wheelDegを採用し、150 mm接近した。その後の近距離走査は往路21件、復路33件すべて有効で、未検知ではなかった。往路候補-41 wheelDeg・126 mmと復路候補-71 wheelDeg・137 mmの差が約19度となり、従来の5度一致条件により後退した。後退後の再探索は未検知で終了した。

復路候補は元の接近方位-68 wheelDegから約2度しか離れていない。大きな方位変更の不一致を対象ロストと同一扱いすることが、不要な後退につながった可能性がある。往復不一致でも復路候補が元の接近方位から10度以内なら、その候補の方位と距離を採用するよう変更した。大きな変更の往復確認、復路未検知時の後退、距離フィルター、前進上限、速度は維持する。

`robot_log_20260912_145141.txt` は最終半角90度の往復を含め、全走査が有効0件だった。操作者の「1・2回目とも高精度検出へ進んだ」という観察とは2本目のログが一致していない。今回の採否変更では未検出自体を改善できないため、別の課題として残す。

ホスト回帰テストでは今回の候補遷移-68 → -41 → -71 wheelDeg、元の方位から±15 wheelDegの採用と±18 wheelDegの不一致時後退、復路未検知時の後退、往復一致した大角度補正の採用を確認する。サンプル数による多数決は導入しない。この候補遷移の再現は実際の反射や押し出し成功の証明ではない。

次の実機試験では `near correction keep heading` の後に `near align target` が続くこと、実際に押し出せることを確認する。これまで後退したケースで接近を継続する変更のため、誤反射や押し外しが増えないかも記録する。
# 超音波センサー特性評価

## 目的

`UltrasonicAlignTracer` の探索アルゴリズムを変更する前に、`UltrasonicProbeTracer` を用いて
PORT_F の超音波センサーを特性評価します。Probe は制御周期ごとにロボットを停止させ、
`pup_device_get_values()` の戻り値を直接ログへ出力します。

センサーの取付位置は大会規約で固定されています。これらの計測および大会本番の構成において、
センサーを上下その他の方向へ移動させないでください。

## Probe の実行

`tracer.ini` に Probe の行を1つ設定し、無関係な走行 Tracer はコメントアウトしてください。

```text
UltrasonicProbeTracer DISTL 300 100
```

書式は次のとおりです。

```text
UltrasonicProbeTracer <DISTL|DISTS|TRAW|ADRAW|ALL> <sample_count> [sample_interval_ms]
```

`sample_interval_ms` の既定値は 100 ms です。10 ms の制御周期単位へ切り上げられます。`ALL` は
DISTL、TRAW、ADRAW の順に読み取ります。モード切替ごとに 50 ms 待機し、1回読み捨ててから値を
記録します。このため `ALL` の1周期は指定したサンプル間隔より長くなります。実際の周期時間は
`[ULTRA_PROBE_CYCLE]` で確認してください。

Bluetooth 出力は PC 側に保存してください。記録値は1行1サンプルの TSV 形式です。

```text
[ULTRA_PROBE]\tseq\tmode\terr\tvalue\telapsed_ms
```

`err` は `pbio_error_t` の戻り値です。従って `err == 0, value == -1` は有効な距離を取得できなかった
センサー読取り値であり、`err != 0` は PUP 読取り失敗です。`[ULTRA_PROBE_CYCLE]` には、モード一巡ごとの
シーケンス番号と経過時間（ms）が出力されます。

## 計測

各条件で Bluetooth 出力を30秒以上記録してください。変化させる要因以外は、固定されたセンサー取付位置、
ロボット方位、ボトル姿勢、床面条件を実験記録に残してください。

| ID | 手順 | 記録する結果 |
| --- | --- | --- |
| E1 | ボトルを 200、300、400、500、600、800 mm に置き、静止状態で DISTL を測定します。 | 距離ごとの検出率ベースライン。 |
| E2 | 実施不可: 大会規約で取付位置が固定されているため、センサー高さ比較は除外します。対照記録が必要な場合だけ、同じ固定位置で E1 を繰り返します。 | 高さ比較を実施していないこと。 |
| E3 | E1 で有用だった各距離で `ALL` を実行します。 | DISTL が `-1` のときに TRAW または ADRAW が変化するか。 |
| E4 | 300 mm と 500 mm で DISTS を実行します。 | DISTL と比較した DISTS の検出率。 |
| E5 | 400 mm でモーターを停止したまま、操作者がロボットを -20 から +20 mm まで 5 mm 刻みで並進させ、各位置で DISTL を実行します。 | 並進位置による検出率の変動。 |
| E6 | 有用な固定距離でモーターを停止したまま、操作者がロボットを +/-60 度、35度/s で手動旋回させます。続けて 8 度刻み・各2秒静止の測定と比較します。 | 連続旋回と静止測定の検出率。 |
| E7 | 同じ距離でボトルを 40 mm 角の LEGO プレートに置き換え、DISTL を実行します。 | 平板に対するボトル固有の損失。 |

E3 と E6 を優先して実施してください。固定取付の規約により、E2 は Phase 2 の判断基準には使えません。

## 結果テンプレート

### 検出率

`err == 0` かつ `50 <= value < 2500` を DISTL の検出として数えます。`err == 0` の `value == -1` は
エコーなしであり、ドライバエラーではありません。900〜2000 mm の有効な測定値を捨てないでください。

| 実験 | 対象 | 距離 mm | モード | サンプル数 | `err == 0` | エコー数 | 検出率 | 備考 |
| --- | --- | ---: | --- | ---: | ---: | ---: | ---: | --- |
| E1 | ボトル |  | DISTL |  |  |  |  |  |

2026-08-16 の概略計測結果です。実験中の条件切替マーカーがログにないため、E6 の連続旋回と
静止ステップを厳密に分離した比較値ではありません。

| ログ | 実験相当 | モード | サンプル数 | `err == 0` | エコー数 | 検出率 | 備考 |
| --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| `robot_log_20260816_161347.txt` | E1（検出圏外） | DISTL | 300 | 300 | 0 | 0.0% | 全件 `-1` |
| `robot_log_20260816_161632.txt` | E3（静止） | DISTL | 300 | 300 | 291 | 97.0% | 距離値は主に382〜396 mm |
| `robot_log_20260816_162619.txt` | E6（手動走査） | DISTL | 162 | 162 | 51 | 31.5% | 角度条件の記録なし |

E6 相当ログでは、DISTL が `-1` の111件中43件で TRAW が `134217727` でした。ADRAW の平均は
DISTL ヒット時122.3、`-1` 時61.1で相関は見られますが、値の重なりと TRAW センチネルがあり、
今回のログだけから安定した独自距離閾値を定義することはできません。

### Phase 1 の結論

- `-1` の由来: 全サンプルで `err == 0` だったため、PUPエラーではなくDISTLの無反射値。
- 弱いエコーに対する TRAW/ADRAW の有用性: 相関はあるが、Phase 2で距離判定へ使える水準とは判断しない。
- 連続旋回と静止測定の比較: 厳密な条件分離は未実施。手動走査中にも検出バーストが得られたため、連続旋回を暫定採用する。
- センサー取付位置: 大会規約に従い固定を維持する。
- Phase 2 の判断: DISTLの1-of-M判定、約35度/sの連続往復走査、方位占有ヒストグラムで開始する。

Phase 2 実機確認では、走査ログへ角度と走査方向を残し、連続旋回の検出率を再評価してください。

## 2026-09-12 探索改善の比較試験

### 根拠と未確認事項

`robot_log_20260912_132303.txt` では開始13:23:25.276、初回往路終了28.574（15ヒット）、復路終了31.875（43ヒット）、確認往復後の目標決定35.115でした。目標決定まで約9.8秒かかっています。初回往路に候補があるため復路を省く余地がありますが、実際に削れる時間と正対誤差は変更後の走行で測定してください。

指示35度/sに対する局所速度ログは12〜51度/sでした。35度/sが最適とも、設定を上げれば比例して速くなるとも未確認です。往復で中心角が変わる可能性があり、片道化による方位の偏りも評価します。

旋回時の後退は、左右回転量の不均衡、タイヤの滑り、明示的な後退制御を分離します。新しい `sweep summary` の `drift` はエンコーダ平均の変化であり、実位置変化そのものではありません。上からの動画と開始位置マークを併用してください。`backup before rescan`、`push lost`、`return begin` に対応する後退は意図した動作です。

`ultrasonicsensor_approve.md` のマルチパス原因、位相反転に必要な移動量、35度/s推奨は、今回の取付・床・ボトル条件で原因まで実証した結論ではありません。試験上の仮説として扱い、取付位置は変更しません。今回の改修はDISTLの感度やモードを変更せず、車輪角合計保持、候補ありの復路省略、未検知時の測距付き50 mm前進で改善を試みます。

### 最小試験セット

距離はセンサー面からボトルまで、方位はトレーサ開始時の正面基準で記録します。ボトル、床、照明、バッテリー残量、ログレベルを揃え、試験条件ごとにログファイルを分けます。全走行を記録し、失敗試行も除外しません。

| 順序 | 条件 | 回数と判定 |
| --- | --- | --- |
| 1 | 旧版と改修版、ボトル250 mm・正面、走査35度/s | 各5走。探索から押し出し完了までの時間、接近開始前の車体中心ずれ、正対誤差を動画とログで比較。まず実移動と `drift` が一致するか確認。 |
| 2 | 改修版、350 mm・正面、方位+30度、-30度 | 各5走。初回未検知からの捕捉率、クリープ回数、押し外し、接触前の後退を記録。配置上安全なら450 mmも追加。 |
| 3 | 代表的な難条件を固定し35/45/55度/sで比較 | 各10走を速度順に偏らない順番で実施。成功率、時間の中央値・最大値、正対誤差、再探索回数を比較。指示速度と実測速度を分けて記録。 |

旧版は現在の変更を退避・破棄して切り替えず、既存ログか別作業ディレクトリで用意した比較用ビルドを使用します。初期の少数試行は候補速度の選別用であり、競技成功率の保証にはなりません。成功数/試行数を併記し、速くても押し外しが増える設定は採用しません。最終候補は実際の配置誤差・バッテリー範囲で追加反復します。

350 mmで再捕捉できない場合だけ、静止Probeによる原因切り分けを追加します。

```text
UltrasonicProbeTracer DISTL 300 100
```

ボトル距離250/300/350 mmを各30秒測り、背景だけの試行も1本記録します。エコー率だけでなく初回有効値までの時間、最長連続 `-1` 時間、距離中央値を比較してください。背景反射をボトル検出に数えないため、既知距離から外れた有効値を別集計します。DISTS比較をする場合は250/300 mmのみで始め、モードの測定レンジと切替待ちを守ります。未文書のTRAW/ADRAW値による独自閾値はまだ採用しません。

### 安全条件と記録表

クリープは最大50 mmを5回、合計指令250 mmです。開始方位へ戻って前進するので、斜め方向へ見失ったボトルを直接追う動きではありません。土俵端、別ボトル、障害物との余裕がこの前進と終了時150 mm後退を許すことを確認し、操作者が停止できる状態で試験してください。全走行上限は二次元の土俵境界を保証しません。

| 版 | 距離 mm | 方位 deg | 指示 deg/s | 成功/試行 | 完了時間 中央/最大 s | 再探索回数 | 実位置ずれ mm | 備考 |
| --- | ---: | ---: | ---: | --- | --- | --- | --- | --- |
| 旧版/改修版 | | | | | | | | |

確認したい運用条件: 通常の開始距離・方位誤差、許容される前進と後退の距離、許容探索時間、誤検出し得る背景物体。この4点が確定するまで、探索半角とPWMは自動的に増やしません。

### 速度比較の実機報告とログ分析

操作者報告: 35/45/55/85/75度/sを試走し、85度/s以外は成功。高速になるほどボトルの右側を捉え、余裕が小さく見えた。各速度の反復回数、初期配置の一致、真のボトル中心角は未確認であり、以下は成功率の推定ではなく個別走行の記録。

時間はPC受信時刻の `start` から最初の `approach begin`、および `return begin` まで。後者は終了時の退避を含まない。Bluetoothのバッファリングによる誤差を含む。

| ログ末尾 | 指示度/s | 最初の接近まで s | 退避開始まで s | 結果 | 備考 |
| --- | ---: | ---: | ---: | --- | --- |
| 134455 | 35 | 10.32 | 15.30 | 押し出し完了ログあり | 初回未検知、50 mm前進後に捕捉 |
| 134745 | 35 | 5.57 | 11.99 | 成功 | 初回走査で捕捉 |
| 135048 | 45 | 5.22 | 9.60 | 成功 | 初回未検知、クリープ中に捕捉 |
| 135217 | 55 | 4.86 | 9.29 | 成功 | 初回未検知、クリープ中に捕捉 |
| 135355 | 85 | 3.36 | 25.62 | 失敗 | 捕捉後にロスト、回復失敗 |
| 135702 | 75 | 3.78 | 10.14 | 成功 | 近距離で方位が負角側へ移動 |

75度/sの近距離走査は294 mmの候補が6個に分裂し、元の狙い方位-2 wheelDegから-23 wheelDegへ約13.5度移動した。85度/sでは294 mmの候補が8個に分裂し、-50から-80 wheelDegへ約19.3度移動した後、無反射が続いた。世界座標上の左右と真の中心角はログのみでは確定しないが、同距離候補を負角側から先着順で選ぶコードの偏りは確認できた。

この選択を「距離優先、同じ中央値なら走査中心に近い候補」に修正した。記録された近距離ビン配置の回帰テストでは75度/s相当で1 wheelDeg、85度/s相当で-47 wheelDegが選ばれる。元ログは各ビン内の全生サンプルを含まないため、同距離配置を再現した局所テストであり、修正後の実走行結果を保証するものではない。35度/sの粗探索など、ほかの同距離候補選択にも適用される。

ビン幅3 wheelDeg、読み取り30 msでは、約64度/sを超えると等速旋回の1読取りで1ビン以上進む。75度/sなら3.5 wheelDeg、85度/sなら約4.0 wheelDegとなり、未読ビンが生じ得る。現行ヒストグラムは未読と無反射を区別せず連続ビンで対象を分割するため、高速時の分裂を検出失敗と同一視できない。今回の同距離修正ではこの構造とセンサー遅延は変更しない。同一値の連続も更新時刻の証明にはならず、固定遅延による角度補正は保留する。

今回の走査中 `drift` は-4〜+3 wheelDeg（エンコーダ平均で約-2.0〜+1.5 mm）だった。滑りと走査外の移動は含まないので、実位置の安定性は引き続き動画で確認する。

次の比較は修正版の55度/sと75度/sを同一配置で交互に各5走。右寄り、押し外し、再探索回数、退避開始までの時間を比較する。75度/sで余裕が改善するまでは85度/sを競技設定にしない。偏りが残る場合は、未読ビンの扱いと、粗探索を高速・最終正対のみ低速にする方式を別々に評価する。新しい静止Probe測定より、まずこの既知の選択偏りを除いた動的比較を優先する。

### 同一配置での55/75度/s交互試験（14時台）

操作者は各3走を同一配置で交互に試行し、最後の75度/sのみ失敗と報告。右寄りとはロボットから見た進行方位の右側であり、ボトルを通過してからの再探索では復帰できなかった。`140540` は接続記録5行のみで、最初の55度/sの走行をログで検証できない。`140610`（75）、`140801`（55）、`140919`（75）、`141108`（55）、`141228`（75）の5走を確認した。75度/sの最初の走行は捕捉までにクリープ5回を要しており、速い旋回が短い所要時間を保証しない。

最後の `141228` はコード上のPUSHING開始前に失敗している。14:12:49.184に352 mm・方位-50 wheelDegを選択し、150 mm接近後の近距離走査で未検知。14:12:51.580の接近再開時に、古い352 mmの取得位置を現在位置へ更新してしまい、追加150 mmを指令した。停止測定は無反射だが予測距離は195 mmとなった。本来は最初の150 mm分も差し引く必要があり、対象を通過した後の誤った距離予測に繋がり得る。

修正内容:

- 古い距離の取得位置を接近開始時に上書きしない。取得から100 mm超移動した場合は即時パルスを行わない。
- 近距離走査の往復が未検知なら接近方位に戻り、直前パルス分（最大150 mm）後退して再探索。回復失敗後の追加クリープを禁止する。
- 押出中の遠い反射（240 mm超）は採用上限外でも100 mm後退の対象。回復後退は共有で最大3回。`-1` だけのロスト判定は盲域との識別が未確立なので保留し、押出中の生値ログを追加。
- 粗探索は指定速度を維持し、候補確認と近距離正対の目標速度だけ35度/sを上限とする。右寄りの原因を固定遅延と断定した補正ではなく、未読ビンと測距遅延の影響を抑える比較用変更。

次は同一配置・粗探索75度/sで、正対用走査が `scan mode: fine=1 target=35deg/s` になること、ボトル中央を捉えるか、近距離未検知時に追加前進せず戻るかを動画とログで確認する。正常な押し出し中の `push sample` も必要。無反射だけで外れた場合は、その時刻と接触・通過の映像を対応付ける。故意に危険な押し外しは作らず、安全な後方余裕と停止手段を確保する。初回55度/sの走行ログが別ファイルに残っていれば補完する。

### 正対速度分離後の3走（14:20〜14:23）

同じ配置・粗探索75度/sで、操作者報告は1・3回目成功、2回目失敗。3試行だけで成功率の改善を確定するものではない。

| ログ末尾 | 近距離走査 有効/読取り | 方位変更 wheelDeg | 結果 |
| --- | --- | --- | --- |
| 142049 | 24/24 | -14 → -23（約-5.8度） | 成功。探索開始から退避開始まで約16.26秒 |
| 142159 | 2/23 | -14 → +13（約+17.4度） | 近距離正対後に通過、回復失敗。約27.00秒で失敗退避開始 |
| 142304 | 25/25 | -26 → -26 | 成功。約9.42秒で退避開始 |

2回目は走査開始角17 wheelDegに近いビン51..52（13、16 wheelDeg）で286 mmが2回だけ得られ、残り21読取りは無反射だった。同じ値を30 ms間隔で2回読んだだけで、独立した2回のピングとは限らない。単独の端側候補へ17.4度向き直して150 mm進み、その後ロストした。今回もPUSHING開始前であり、前回の「近距離走査が全未検知」の後退条件には該当しなかった。端側のエコーが残留値、実反射、背景反射のいずれかはログだけでは確定できない。

修正: 近距離の補正角が10度を超えた場合だけ、往路ヒストグラムを破棄し、復路のヒットだけで角度を再評価する。往路候補との角度差5度以内なら採用し、未検知・不一致は後退復帰へ送る。候補の存在は1-of-Mのまま、危険な大角度変更だけに確認時間を使う。多数決やヒット数下限は導入しない。復路のヒットも時間的に独立と保証されるわけではなく、同一の誤反射を繰り返す可能性は残る。

速度の注意: 失敗試行の近距離走査は `fine=1 target=35deg/s` だが、区間速度ログは57度/s・PWM22。走査時間も67制御周期（約0.67秒）で、約40度の幅を35度/sで走査する理想時間約1.14秒より短い。PWM下限により指令速度へ減速できない区間がある。今回の修正では速度下限は変更せず、条件付き復路確認を先に比較する。次の課題は正対速度制御の下限と初期PWMの実測調整。

正常押出例 `142304` の `push sample` は19件で77〜85 mmだった。正常時に近距離値が持続する一例は得られたが、これだけでは全条件での `-1` のロスト閾値は決められない。

次は同じ配置・設定75度/sで3走し、`near correction verify` が出た試行だけ、復路で同方位を捕捉できるか、`near correction inconsistent` または未検知時に追加150 mm前進せず戻るかを確認する。1・3回目のような小補正では確認の復路が増えないことも見る。生の反射率だけでなく、押し出し成功と全体時間を記録する。

### 条件付き復路確認後の報告（14:29〜14:33）

操作者は試行数を5走から4走へ訂正し、「今回の4走は、実際の押し出しまで全部成功したと思います」と報告。以下の4本のログと試行数は一致し、不足ログはない。全4走で `pushed=1` が記録され、実機での押し出しも全走成功との暫定的な目視報告として扱う。再探索が多くなったという観察も記録する。

| ログ末尾 | 開始から最初の接近まで s | 開始から退避開始まで s | クリープ回数 | 接近前の候補確認失敗 | 大角度確認から後退 | 終了ログ |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 142937 | 18.24 | 30.24 | 4 | 1 | 1 | pushed=1 |
| 143045 | 7.14 | 13.50 | 1 | 0 | 0 | pushed=1 |
| 143204 | 11.88 | 17.22 | 2 | 1 | 0 | pushed=1 |
| 143304 | 13.07 | 18.12 | 2 | 1 | 0 | pushed=1 |

全4走で初回粗探索は候補なしでクリープへ移行。3走で接近前の候補確認が往復とも未検知となり、同位置の再探索へ移った。前回の大角度確認は初回の1走だけで作動し、往路-14、復路-32 wheelDegの差が許容5度を超えて後退した。その後は再捕捉し、近距離目標-44 wheelDegで押し出し完了判定に至った。この後退が不要だったかどうかは、反射の真の方位が不明なため断定しない。

この4走の時間増大は接近前の探索が大きな部分を占め、大角度確認だけでは説明できない。退避開始までの中央値は約17.67秒。今回の観測結果は4走とも完了判定・目視報告上も成功だが、少数試行かつ目視報告は暫定的なため、一般的な成功率や以前に対する性能差を確定しない。

この報告を記録した時点では制御・閾値・設定値を変更しなかった。その後、接近前の同位置探索時間の短縮を依頼されたため、以下の変更を実施した。比較対象は今回の4走とし、押し出し成功を維持しながら接近前の探索時間を減らせるかを評価する。

### 接近前の同位置再探索を省略する変更

候補確認の往復が未検知で終わった場合、接近開始前・有効な最終距離あり・予測距離200 mm超・クリープ枠あり・次の50 mmが全体前進上限内、のすべてを満たすと同位置再探索を省いて測距付き50 mmクリープに移る。最後のエコー位置からの走行量を引いて判定し、クリープ開始時には古い候補を破棄する。候補確認の往復そのものは省略しない。

新ログ `target verify found nothing; translate` の直後に `creep` が出ることで識別する。近距離、接近開始後、枠不足なら従来の `target verify found nothing; rescan` を維持する。後退回復中の未検知終了、大角度正対の復路確認は変更しない。50 mmを最大5回というクリープ総枠と設定値は据え置く。

前回4走では接近前の確認失敗後、同位置再探索で未検知のまま終わるケースと、再捕捉するケースの両方があった。前進に切り替えると、前者の待ち時間を減らせる可能性がある一方、後者では不要だった前進を行う可能性もある。ログの時間差をそのまま短縮保証には使わない。

ホスト回帰テストでは、予測距離201/200 mmの境界、近距離120 mm、無効な最終値、移動後の予測距離、5回枠消費済み、全体前進上限、接近開始後、後退回復中を確認する。

次は同じ配置・75度/sで4走比較する。実際の押し出し成功、開始から最初の `approach begin` までの時間、開始から `return begin` までの時間、直接クリープへの切替回数と総クリープ回数を記録する。基準4走の退避開始までの中央値は約17.67秒、最大30.24秒。新分岐が出ない試行だけでは今回の短縮効果を評価できない。近距離で不適切な前進や押し外しが増えた場合は速度をさらに上げず、該当ログで条件を再評価する。

### 最遠配置の3走（14:41〜14:45）

操作者回答: 距離400〜500 mm前後、ロボットから見て右30度弱。安全に前進できる合計距離は測定待ちのため、250 mmの指令枠は増やさない。

| ログ末尾 | 記録された挙動 | 結論 |
| --- | --- | --- |
| 144154 | 14:42:39.714の4回目 `creep` 計画後、タスクログも途絶え、約26秒後に切断 | 正常終了ログなし。旋回・モーター・センサー待ち・タスク・通信のどこで停止したか未確定 |
| 144307 | 5回前進、7走査で有効0・無反射384・範囲外0。最後の前進量は496 wheelDeg（約242 mm） | 探索回数上限で失敗退避し、次のTracerへ移行 |
| 144443 | 5回前進、全7走査で有効0・範囲外0。最後の前進量は495 wheelDeg（約241 mm） | 同じく探索上限で失敗退避 |

3走とも候補確認まで進んでおらず、前回追加した `target verify found nothing; translate` の短縮効果を評価できる試行ではない。採用上限500 mmで有効値を捨てていたわけでもない。

単純な平面幾何では初期距離400〜500 mm・角度30度から正面へ250 mm移動すると、相対方位は約54〜64度、残距離は約222〜310 mmとなる。センサーの旋回中心からのずれ、実角度、滑りを無視した概算であり、実際に範囲外だった証明ではない。従来の固定半角60度では一部配置を外し得る。また、500 mm側では残距離が依然300 mm付近で検出が不安定な可能性もある。

修正は5回目の前進完了後だけ探索半角を90度へ広げる。通常の1〜4回目は設定半角を維持し、前進総量と速度を増やさない。単純計算で75度/sなら片道幅の追加60度は約0.8秒分だが、実所要時間は加減速・ログ・候補確認に依存する。別物体への誤検出と、至近対象がある場合の旋回干渉に注意する。

停止切り分けのため、開始方位への旋回完了時に `creep drive begin` を追加した。1回目の停止原因を直したとは扱わない。全タスクログが途絶える場合、同一タスク内のタイムアウト追加だけでは停止を検知できない可能性がある。

次の実機試験では最遠配置と設定75度/sを維持し、最後の `translated search` が `half=140 wheelDeg` になるか、側方で捕捉するかを確認する。停止が再発したら、車輪が停止・振動・回転のどれだったか、Bluetooth接続状態とハブ表示、最後の `creep` / `creep drive begin` を記録する。前進可能距離の実測値が得られるまで、追加前進や斜め方向への探索移動は導入しない。